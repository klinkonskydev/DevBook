BASE_URL="http://localhost:5000"

# Demo account seeded by api/sql/data.sql - every seeded user shares this
# password, so you can log in as any of them (see api/sql/data.sql).
DEMO_EMAIL="ana.silva@example.com"
DEMO_PASSWORD="123456"

# Another seeded user, used by the follow/unfollow examples.
OTHER_USER_ID="2"

# Filled in automatically once you run collection/login/login.posting.yaml
# and collection/posts/create-post.posting.yaml - no need to edit these by
# hand, they're just declared here so requests can reference them.
TOKEN=""
USER_ID=""
POST_ID=""
