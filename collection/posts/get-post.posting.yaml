name: Get Post
description: Requires $TOKEN (run Login first). Fetches the post created by "Create Post".
url: $BASE_URL/posts/$POST_ID
headers:
- name: Authorization
  value: Bearer $TOKEN
- name: content-type
  value: application/json
